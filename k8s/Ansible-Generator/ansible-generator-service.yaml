apiVersion: v1
kind: Service
metadata:
  name: ansible-generator-svc
  namespace: default
  labels:
    app: bram-vortex-ansible-generator
spec:
  selector:
    app: bram-vortex-ansible-generator
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP