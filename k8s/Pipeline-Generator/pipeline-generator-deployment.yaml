apiVersion: apps/v1
kind: Deployment
metadata:
  name: bram-vortex-pipeline-generator
  namespace: default
  labels:
    app: bram-vortex-pipeline-generator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bram-vortex-pipeline-generator
  template:
    metadata:
      labels:
        app: bram-vortex-pipeline-generator
    spec:
      # Χρήση του εξουσιοδοτημένου Service Account
      serviceAccountName: pipeline-sa
      containers:
        - name: pipeline-generator
          image: christosbramis/bram-vortex-pipeline-generator:fa8daed8158058acce1613852897d8d50d096064
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            # Endpoint Βάσης Δεδομένων
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres-svc:5432/vortexdb"

            # Credentials Βάσης από το CSI Bundle
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: pipeline-secrets-bundle
                  key: pp_user
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: pipeline-secrets-bundle
                  key: pp_pass

            # Gemini API Key από το CSI Bundle
            - name: SPRING_AI_GOOGLE_GENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: pipeline-secrets-bundle
                  key: google-api-key

            # JPA Settings
            - name: SPRING_JPA_HIBERNATE_DDL_AUTO
              value: "update"
            - name: SPRING_JPA_DATABASE_PLATFORM
              value: "org.hibernate.dialect.PostgreSQLDialect"

          volumeMounts:
            - name: secrets-store-inline
              mountPath: "/mnt/secrets-store"
              readOnly: true

      volumes:
        - name: secrets-store-inline
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: "pipeline-vault-provider"